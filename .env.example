# Copy this file to `.env` and fill in the values as needed.

# Admin token for protected endpoints (generate a strong random string)
# Generate example: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_TOKEN=!CQ[K>|0qanS4CIxl:+a.%5^oUShm08J>zJ<z0(q#MTe

# Default crawl source URL (leave blank to disable fetching)
CRAWL_URL=https://www.xn--2q1bm5w1qdbqaq6cwvm.com/wp-content/themes/welcomecar-new/auction_data.csv

# Local directory used to store downloaded CSV files (relative to project root)
SOURCES_DIR=sources

# Optional overrides
# APP_NAME=Car Auction API
# APP_VERSION=1.0.0

# Cloud Spanner configuration (enable to store CSVs in Spanner)
SPANNER_ENABLED=false
# Primary project hint (falls back to ADC default project when empty)
SPANNER_PROJECT=
# Required: instance id and database id where the table lives
SPANNER_INSTANCE=car-auction-250926
SPANNER_DATABASE=car_auction_spanner
# Table that stores CSV payloads (STRING date key, BYTES content, etc.)
SPANNER_TABLE=auction_data
# Uncomment to connect to emulator (e.g., localhost:9010)
# SPANNER_EMULATOR_HOST=

# Shared Google Cloud hints (optional)
# GCP_PROJECT=
# GCP_PROJECT_ID=

# Google credentials
# GOOGLE_APPLICATION_CREDENTIALS=./etc/car-auction-471105-41e093c4e037.json
# GCP_SA_KEY=./etc/car-auction-471105-41e093c4e037.json
