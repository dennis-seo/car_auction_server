# Copy this file to `.env` and fill in values.

# Strong random string for admin-protected endpoints
# Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_TOKEN=!CQ[K>|0qanS4CIxl:+a.%5^oUShm08J>zJ<z0(q#MTe

# Crawl target URL (defaults to this if unset)
CRAWL_URL=https://www.xn--2q1bm5w1qdbqaq6cwvm.com/wp-content/themes/welcomecar-new/auction_data.csv

# Where to store/download files (relative to project root or absolute path)
SOURCES_DIR=sources

# Optional
# APP_NAME=Car Auction API
# APP_VERSION=1.0.0

# Firestore (optional)
# Enable to store/read crawled CSVs from Firestore instead of local filesystem
FIRESTORE_ENABLED=false
# If empty, client tries to detect project from Application Default Credentials
GCP_PROJECT=
FIRESTORE_COLLECTION=auction_data
# For local dev, set to a service account JSON path so the SDK can authenticate
# GOOGLE_APPLICATION_CREDENTIALS=/abs/path/to/your-sa.json
